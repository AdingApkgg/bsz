PORT=8080

# Set a non-empty token to enable /api/admin/*. When empty, the admin
# routes are not mounted at all — set this only if you intend to use the
# admin frontend (../frontend/).
ADMIN_TOKEN=

SAVE_INTERVAL=30
MAX_BODY_SIZE=100MB
